import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "api-dev.ziyadbooks.com",
      "media.ziyadbooks.net",
      "images.unsplash.com",
      "mms.img.susercontent.com"
    ],
  },
};

export default nextConfig;